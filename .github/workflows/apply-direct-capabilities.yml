name: Direct capability smoke

on:
  push:
    branches:
      - feat/android-direct-image-native

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Smoke
        run: echo ok
